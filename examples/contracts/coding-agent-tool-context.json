{
  "context_contract_version": 1,
  "id": "coding-agent-tool-context",
  "version": "1.0.0",
  "description": "Application-owned facts required by the coding-agent enforcement boundary.",
  "fields": {
    "tool_context_version": {"type": "integer", "required": false},
    "actor": {"type": "object"},
    "action": {"type": "object"},
    "action.kind": {"type": "string", "required": false},
    "action.operation": {"type": "string"},
    "action.capabilities": {"type": "array", "items": "string"},
    "action.arguments": {"type": "object"},
    "context": {"type": "object"},
    "context.workspace_contained": {"type": "boolean"},
    "context.approval": {"type": "object", "required": false},
    "context.approval.approved": {"type": "boolean", "required": false}
  }
}
