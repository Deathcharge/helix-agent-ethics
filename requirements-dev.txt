# Direct development tool requirements. Compile these into requirements-dev.lock.
# The package itself has no runtime dependencies and is installed separately.
build==1.5.0
mypy==1.19.1
pytest==8.4.2
pytest-cov==6.3.0
ruff==0.15.12
twine==6.2.0
